/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['flowbite.com', 'www.naraicoder.org', 'chatgpt.com', 'info.naraicoder.org'],
      },
};

export default nextConfig;
